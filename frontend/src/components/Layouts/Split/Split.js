import React from "react";
import useSplit from "./Split.hook";

const Split = (props) => {
    const {} = useSplit();
    return <React.Fragment>It's working Split</React.Fragment>
};

export default Split;
